{
    "title": "Career Site Job Listing API",
    "description": "Extract public listings from 18 ATS platforms. Paste board or company careers URLs for URL, page-markup, and embedded-data detection.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Job Board URLs",
            "type": "array",
            "description": "Add public ATS boards or employer careers pages. Pages are checked for ATS links, embedded endpoints, vendor markers, and recognizable public job-data schemas.",
            "editor": "stringList",
            "prefill": [
                "https://jobs.lever.co/spotify"
            ],
            "minItems": 1,
            "items": {
                "type": "string"
            }
        },
        "results_wanted": {
            "title": "Maximum number of jobs",
            "type": "integer",
            "description": "Maximum number of job listings to collect.",
            "minimum": 1,
            "default": 20,
            "prefill": 20,
            "editor": "number"
        },
        "max_pages": {
            "title": "Maximum pages",
            "type": "integer",
            "description": "Maximum number of API pages to fetch per paginated board. No fixed upper limit is imposed by the Actor.",
            "minimum": 1,
            "default": 5,
            "prefill": 5,
            "editor": "number"
        },
        "proxyConfiguration": {
            "title": "Proxy configuration",
            "type": "object",
            "description": "Optional proxy settings.",
            "editor": "proxy",
            "default": {
                "useApifyProxy": false
            }
        }
    },
    "required": ["startUrls"],
    "additionalProperties": false
}
